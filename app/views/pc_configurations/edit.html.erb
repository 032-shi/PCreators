<h1>PC構成リスト投稿編集フォーム</h1>
<%= form_with model: @pc_configuration, local:true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <h4>説明</h4>
  <%= f.text_area :body %>
  <%= f.submit '変更を保存する' %>
<% end %>