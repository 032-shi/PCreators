<%= form_with model:[post, post_comment] do |f| %>
  <div><%= f.text_area :comment, rows:'5', cols:"30", placeholder: "コメントしましょう" %></div>
  <div><%= f.submit "コメントする", class: "btn btn-success" %></div>
<% end %>