var post_index_title = document.getElementById("user-show__post-index__title");
$('#user-show__post-index').html("<%= j(render 'users/post_index', posts: @posts) %>");
post_index_title.scrollIntoView({
  behavior: "smooth"
});
